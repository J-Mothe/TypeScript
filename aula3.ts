// Declarando nossa variável

let logo : string = "AULA DE TI"; // TODA STRING TEM QUE ESTAR ENTRE ASPAS
let www : string = "www";
let dominio : string = "auladeti";
let tipo : string = "com.br";

//Concatenando strings
let site = www + "." + dominio + "." + tipo;

// Imprimindo o valor de nossa variável
console.log(site);
console.log(logo);