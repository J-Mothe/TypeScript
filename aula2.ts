// Declarando nossa variável 
let x : number = 3.14;
let y : number = 2;

//Realizando os cálculos
let soma = x + y; // Soma X + Y

let subt = x - y; // Subtrai X - Y

let mult = x * y; // Multiplica X por Y

let expo = x ** y; // Eleva X à Y

let mod = x % y; // Captura o resto

// Imprimindo o valor de nossa variável
console.log ("Soma: " + soma);
console.log ("Subtração: " + subt);
console.log ("Multiplicação: " + mult);
console.log ("Exponencial: " + expo);
console.log ("Resto: " + mod);